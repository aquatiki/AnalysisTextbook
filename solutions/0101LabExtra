`Intra' means `within' and `extra' means `outside'.  Interpolation is finding a new data point between existing ones.  Extrapolating is going beyond existing data to project new possibilities, such as 100 items.
